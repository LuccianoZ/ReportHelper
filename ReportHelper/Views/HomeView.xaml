<UserControl x:Class="ReportHelper.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Background="#F8FAFC">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="320">

            <TextBlock Text="Report Helper"
                       FontSize="32" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="#0F172A"
                       Margin="0,0,0,8"/>

            <TextBlock Text="Voice-guided police report workflow"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Foreground="#64748B"
                       Margin="0,0,0,48"/>

            <!-- SC-01: Start New Report button is present on the home screen -->
            <Button Content="Start New Report"
                    Command="{Binding StartNewReportCommand}"
                    Padding="0,16"
                    FontSize="15"
                    Margin="0,0,0,12"/>

            <!-- SC-01: View Past Reports button is present on the home screen -->
            <Button Content="View Past Reports"
                    Command="{Binding ViewPastReportsCommand}"
                    Padding="0,16"
                    FontSize="15"/>

        </StackPanel>
    </Grid>
</UserControl>
